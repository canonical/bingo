# Vendored charm libraries pulled from Charmhub (not authored in this repo).
# Marking these as vendored excludes them from GitHub's language stats and
# collapses them by default in PR diffs.
charm/lib/charms/** linguist-vendored

# Planning/spec notes generated by the superpowers skill workflow.
# Marking these as documentation excludes them from language stats and
# collapses them by default in PR diffs.
docs/superpowers/** linguist-documentation

# Auto-generated lock files - never hand-edited, fully collapsed in PR diffs.
web/package-lock.json linguist-generated
charm/uv.lock linguist-generated
go.sum linguist-generated
